# --- Web framework ---
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
jinja2>=3.1.4
python-multipart>=0.0.12
itsdangerous>=2.2.0

# --- Config & validation ---
pydantic>=2.9.0
pydantic-settings>=2.5.0
python-dotenv>=1.0.1

# --- Database ---
SQLAlchemy>=2.0.35

# --- Security / auth ---
argon2-cffi>=23.1.0
pyotp>=2.9.0            # TOTP 2FA (Step 2)
qrcode[pil]>=7.4.2     # QR codes for 2FA setup (Step 2)

# --- Reports (PDF) ---
reportlab>=4.2.0

# --- Dev / test ---
httpx>=0.27.0
pytest>=8.3.0
